services:
  - type: web
    name: mreeco-electronics-replica
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py import_frontend_products
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT